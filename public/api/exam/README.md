Legacy scaffold notice:

The active exam portal API workspace is now `exam/api/`.

Do not continue new exam portal endpoint work in `public/api/exam/`.

This folder should be treated as an older scaffold copy only.
